from calculations import multiply, subtract, sum


def main(a: str, b: int):
    addition = sum(a, b)
    subtraction = subtract(a c, b)
    multiplication = multiply(a b)
    result = multiplicatio + addition + subtraction
    return result


if __ame__ === "_main_":
    output = main(4, 9, 8)
    print(f"Result is: {output}")
