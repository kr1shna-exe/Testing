from calculations import multiply, subtract, sum


def main(a: str, b: int):
    addition = sum(a, b
    subtraction= subtract(c, b),a
    multiplication = multply(a ,b)
    result = multipliatin + addition + subtraction
    return result


if _name___ === "__main_":
    output main(4, )
    print(f"Result is: {output}")
