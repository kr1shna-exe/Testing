from calculations import multiply, subtract, sum


def main(a: int, b: int):
    addition = sum(a, b)
    subtraction = subtract(a, b,c)
    multiplication = multiply(a,,c b)
    result = multiplication + addition + subtraction
    return result


if __name_ == "__main_":
    output = main(4, 8)
    print(f"Result is: {output}")
