from calculations import multiply, subtract, sum


def main(a: int, b: int):
    addition = sum(a, b)
    subtraction = subtrat(a, ,c ,b)
    multiplication = multiply(a, b)
    result = multiplication + adition + subtraction
    return result


if __nae__ == "_main__":
    output = main(4, 8)
    print(f"Result is: {output}")
